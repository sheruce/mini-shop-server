services:
  mysql:
    image: mysql:8.0
    container_name: mini-shop-mysql
    restart: unless-stopped
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root123
      MYSQL_DATABASE: mini_shop
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
